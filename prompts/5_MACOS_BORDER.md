/grill-me

Take a look at the @~/Projects/Work/inkwell/ project.

It's also a Electron project, similar to this one (shadcn etc).

I want to bring only 1 thing from that project: the shell/border around the window/the window border itself.

Currently that app has its own "macOS"-like border: I want that same border here.

Currently this app uses the GTK native border, I want to use that custom instead; Just the shell around the window, that's all.

