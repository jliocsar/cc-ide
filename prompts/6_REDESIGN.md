/grill-me

I want to redesign this app's look to have a more skeuomorphism in it

1. I don't want to move elements around: they stay where they are.
    - If you see an element in a place where the UX seems weird, suggest improvements/alternatives.
2. I don't want to change any logic: only styles.
3. I want to keep the same colors: unless we make things a bit darker.
4. If you see a place where we could use some backdrop blur/glass look, suggest.

Furthermore, I want to add animations to everything that _can_ be animated:
- Transitions
- Open/close any collapsable component
- Switching tabs (if possible -- as in sliding the new tab in from the left/right)
- Maximizing/minimizing windows
And so on
Important: If the animation/transition can be done via CSS-only -- do it using CSS instead

