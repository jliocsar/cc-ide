/grill-me

1. I want the trash/delete button from Prompts (in Ctrl+K > Open Prompts) to have a confirmation if I really want to delete it
2. Right now I need to click on the "worktrees" and "conversations" accordions for them to run the first time/update the data the first time -- it should do the initial fetching/update when I launch the app too
3. In the "Diffs" accordion, let's put a divider/"---" between each branch/worktree
4. The icons in the statusbar aren't aligned with their text:
    - For example, the magnifying glass icon is a bit below the "100%" text -- this goes to all sections in the statusbar
5. In the "Diffs" accordion, the branch pill component has a slightly larger bottom padding than top, meaning the text isn't centralized -- or has equal paddings around all sizes

