/grill-me

I want to add 2 new features to the Dependency Graph canvas:

1. I want to be able to drag nodes by clicking on them: it should work just like in Obsidian, slowly bringing the rest of the graph with it as I pull
    - It should also highlight the nodes connected to the node I'm dragging (both the lines and the nodes all should be the same highlight color)
2. I want the label/name of each node in the canvas to be smaller/darker color
3. I want each node to have enough room to fit: itself + its label/name + a buffer/room of a few pixels
    - Dragging and dropping nodes should re-adjust them to fit in a space like this
